const array = ['one', 'two', 'three'];

for (let index = 0; index < array.length; index++) {
	const element = array[index];
	console.log(`Element => ${index}: ${element}.`);
}


const cores = ['azul', 'amarelo', 'verde', 'branco'];

for (var i = cores.length - 1; i >= 0; i--) {
	console.log(`=> ${cores[i]}`);
};