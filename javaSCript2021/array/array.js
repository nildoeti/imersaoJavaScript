// array são estruturas para armazenamento de tipos de dados iguais ou diferentes

// array de numeros
let numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]

// array de strings
let cores = ['amarelo', 'verde', 'azul']

// exibindo no console
console.log(numeros); // [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]

// exibindo pelo indíce 
console.log(numeros[0]); // 1

// verificando a quantidade de items no array
console.log(numeros.length);
